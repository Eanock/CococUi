# 贡献指南

感谢你考虑为 WePlayCocos 组件库做出贡献！

## 📋 目录

- [开发环境设置](#开发环境设置)
- [组件开发规范](#组件开发规范)
- [提交流程](#提交流程)
- [代码规范](#代码规范)
- [测试要求](#测试要求)

## 🛠️ 开发环境设置

### 前置要求

- Cocos Creator 3.x+
- Node.js 16+
- Git

### 克隆仓库

```bash
git clone https://gitlab.yourcompany.com/your-group/weplay-cocos-components.git
cd weplay-cocos-components
```

### 安装 CLI 依赖

```bash
cd cli
npm install
npm run build
```

## 📦 组件开发规范

### 1. 目录结构

每个组件应该有以下结构：

```
components/your-component/
├── YourComponent.ts        # 主脚本（必需）
├── YourComponent.prefab    # 预制体（可选）
├── component.json          # 元数据（必需）
├── README.md              # 文档（必需）
└── assets/                # 资源文件（可选）
    ├── textures/
    ├── materials/
    └── ...
```

### 2. 命名规范

- **组件名称**：使用 kebab-case（如：`button`, `loading-spinner`）
- **类名**：使用 PascalCase（如：`Button`, `LoadingSpinner`）
- **文件名**：使用 PascalCase（如：`Button.ts`, `LoadingSpinner.prefab`）

### 3. 创建组件元数据

创建 `component.json` 文件：

```json
{
  "$schema": "../../schemas/component.schema.json",
  "name": "your-component",
  "displayName": "你的组件",
  "type": "ui|logic|effect|utility",
  "description": "简短的组件描述",
  "version": "1.0.0",
  "files": [
    {
      "source": "YourComponent.ts",
      "target": "components/your-component/YourComponent.ts",
      "type": "script"
    }
  ],
  "dependencies": [],
  "tags": ["tag1", "tag2"]
}
```

### 4. 组件类型

- **ui**: UI 相关组件（按钮、输入框、对话框等）
- **logic**: 游戏逻辑组件（状态管理、事件系统等）
- **effect**: 视觉效果组件（粒子、动画等）
- **utility**: 工具类组件（工具函数、辅助类等）

### 5. 编写组件文档

每个组件必须包含 `README.md`，包含以下内容：

```markdown
# ComponentName 组件名称

简短描述

## 特性

- 特性 1
- 特性 2

## 使用方法

### 1. 添加组件

\`\`\`bash
cocos-components add your-component
\`\`\`

### 2. 基本用法

\`\`\`typescript
// 代码示例
\`\`\`

## API

### 属性

| 属性 | 类型 | 说明 |
|------|------|------|
| prop1 | type | 说明 |

### 方法

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| method1 | params | type | 说明 |

## 依赖

列出依赖的其他组件

## 版本

1.0.0
```

## 📝 代码规范

### TypeScript 规范

1. **使用 TypeScript**：所有脚本必须使用 TypeScript
2. **类型注解**：为所有公共 API 提供类型注解
3. **注释**：为公共方法和属性添加 JSDoc 注释

```typescript
import { _decorator, Component } from 'cc';
const { ccclass, property } = _decorator;

/**
 * 组件描述
 */
@ccclass('YourComponent')
export class YourComponent extends Component {
    /**
     * 属性描述
     */
    @property
    public myProperty: string = '';

    /**
     * 方法描述
     * @param param 参数描述
     * @returns 返回值描述
     */
    public myMethod(param: string): void {
        // 实现
    }
}
```

### 命名约定

- **变量/函数**：camelCase
- **类**：PascalCase
- **常量**：UPPER_SNAKE_CASE
- **私有成员**：以 `_` 开头

### 代码风格

- 使用 4 空格缩进
- 使用单引号
- 语句末尾加分号
- 适当的空行分隔逻辑块

## 🔄 提交流程

### 1. 创建分支

```bash
git checkout -b feature/your-component-name
```

### 2. 开发组件

1. 在 `components/` 目录创建组件
2. 编写代码和文档
3. 测试组件功能

### 3. 更新注册表

在 `components/registry.json` 中添加组件：

```json
{
  "name": "your-component",
  "path": "components/your-component",
  "displayName": "你的组件",
  "type": "ui"
}
```

### 4. 提交代码

```bash
git add .
git commit -m "feat: add your-component"
```

提交信息格式：

- `feat:` 新功能
- `fix:` 修复 bug
- `docs:` 文档更新
- `style:` 代码格式调整
- `refactor:` 重构
- `test:` 测试相关
- `chore:` 构建/工具相关

### 5. 推送并创建 PR

```bash
git push origin feature/your-component-name
```

然后在 GitLab 上创建 Merge Request。

## ✅ 测试要求

### 功能测试

1. 在 Cocos Creator 中测试组件
2. 确保所有功能正常工作
3. 测试不同场景下的表现

### 兼容性测试

1. 测试不同 Cocos Creator 版本（如果适用）
2. 测试不同平台（Web、iOS、Android）

### 依赖测试

1. 如果组件有依赖，确保依赖正确声明
2. 测试依赖组件一起使用的情况

## 📋 检查清单

提交前请确保：

- [ ] 代码遵循规范
- [ ] 添加了 `component.json`
- [ ] 添加了 `README.md`
- [ ] 更新了 `registry.json`
- [ ] 在 Cocos Creator 中测试通过
- [ ] 提交信息格式正确
- [ ] 没有包含不必要的文件

## 🐛 报告问题

如果发现 bug 或有建议：

1. 在 GitLab 上创建 Issue
2. 描述问题或建议
3. 提供复现步骤（如果是 bug）
4. 提供环境信息（Cocos Creator 版本等）

## 💡 建议

### 组件设计建议

1. **保持简单**：一个组件做好一件事
2. **可配置**：通过属性暴露配置选项
3. **性能优化**：注意性能影响
4. **文档完整**：确保使用者能轻松上手
5. **考虑复用**：设计时考虑不同场景的复用

### 代码质量建议

1. 遵循 SOLID 原则
2. 避免过度设计
3. 注重代码可读性
4. 添加必要的注释
5. 处理边界情况

## 🤝 代码审查

所有提交都需要经过代码审查：

1. 至少一位维护者审查
2. 通过所有测试
3. 符合代码规范
4. 文档完整

## 📞 联系方式

如有疑问，请联系：

- 团队邮箱：team@example.com
- GitLab Issues
- 内部沟通群

---

再次感谢你的贡献！🎉

